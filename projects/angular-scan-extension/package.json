{
  "name": "angular-scan-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that enables angular-scan on any Angular dev-mode page.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "package:chrome": "pnpm build && cd dist && zip -r ../angular-scan-extension-chrome.zip ."
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0",
    "@types/chrome": "^0.0.270",
    "typescript": "~5.9.3",
    "vite": "^5.4.10"
  }
}
